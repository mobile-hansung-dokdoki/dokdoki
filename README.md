# 똑똑이 — 미니프로젝트 2

고급모바일프로그래밍 주제 **#2109 할 일 앱 — BaaS, AI 활용**의 2단계, **저장되는 앱으로 만들기** 구현입니다. 팀원이 구현한 MP1의 목록·입력·완료 처리와 기존 수정·삭제·검색·필터를 유지하고 로컬 영구 저장, 카테고리 구분, 전체 완료율을 추가했습니다.

평가 범위는 교수님 MP2 안내와 팀의 `똑똑이앱_요구사항_명세서`를 함께 적용했습니다. 명세서에서 우선순위가 ‘중’인 카테고리·검색·완료율도 필수 기능으로 포함했습니다.

## 1. 구현 기능

| 요구사항 | 구현 및 확인 방법 |
|---|---|
| MP1 유지 | 할 일 추가, 완료 토글, 빈 목록 안내 |
| FR-04 수정 | 연필 버튼으로 내용·날짜·카테고리 수정 후 저장 |
| FR-05 삭제 | 휴지통 버튼으로 개별 항목 삭제. 전체 삭제 후에도 빈 목록 저장 |
| FR-06 필터 | 전체 / 진행중(미완료) / 완료 탭으로 전환 |
| FR-07 데이터 유지 | 추가·수정·삭제·완료 변경을 자동 저장하고 앱 재시작 시 복원 |
| FR-08 카테고리 | 미분류(회색), 학교(파란색), 개인(초록색), 약속(주황색). 이름과 색상을 함께 표시 |
| FR-09 검색 | 입력에 따라 내용 또는 표시 형식의 날짜로 즉시 검색. 완료 상태 필터와 함께 적용 |
| FR-10 완료율 | 전체 목록의 완료 개수 / 전체 개수 × 100. 정수로 반올림하고 진행 막대 표시 |

완료율은 검색·필터로 화면에 보이는 항목 수가 아닌 **전체 저장 목록**을 기준으로 합니다. 3개 중 1개 완료는 33%, 전체 완료는 100%, 빈 목록은 0%입니다. 추가·삭제·완료 상태 변경에 따라 다시 계산하므로 별도 저장하지 않습니다.

## 2. 개발 환경 및 실행

| 항목 | 버전 / 환경 |
|---|---|
| Expo SDK | 54 (`expo ~54.0.36`) |
| React / React Native | 19.1.0 / 0.81.5 |
| TypeScript | ~5.9.2 |
| AsyncStorage | 2.2.0 |
| 아이콘 | `@expo/vector-icons ^15.0.3` |
| Node.js | Expo SDK 54 최소 요구인 20.19 이상. 개발 검사는 Node.js 22.23.2에서 수행 |
| 실기기 확인 | 갤럭시 Android + SDK 54용 Expo Go |

프로젝트 루트(`package.json`이 있는 폴더)에서 실행합니다. 제출 ZIP을 풀었다면 `app/` 폴더가 프로젝트 루트입니다.

```bash
npm ci
npm start
```

Windows PowerShell에서 스크립트 실행 정책 오류가 발생하면 다음 명령을 사용합니다.

```powershell
npm.cmd ci
npm.cmd start
```

1. 컴퓨터와 휴대폰을 같은 Wi-Fi에 연결합니다.
2. 휴대폰에 프로젝트 SDK와 호환되는 Expo Go를 설치합니다. Android SDK 54용 설치 파일은 [Expo 공식 다운로드](https://expo.dev/go?sdkVersion=54&platform=android&device=true)에서 받습니다.
3. 터미널의 QR 코드를 Expo Go로 스캔합니다. 앱을 실행하는 동안 개발 서버 터미널을 유지합니다.
4. 설치 후 모듈이 반영되지 않거나 프로젝트를 불러오지 못하면 개발 서버를 `Ctrl+C`로 종료한 뒤 다음 명령으로 다시 시작하고 새 QR 코드를 스캔합니다.

```powershell
npx.cmd expo start --clear
```

MP2는 서버 계정, API 키, `.env` 설정 없이 실행할 수 있습니다. 로컬 목록의 저장·복원은 인터넷을 사용하지 않습니다. 단, Expo Go에서 개발 중인 앱 코드를 새로 불러오는 연결은 컴퓨터의 개발 서버가 필요합니다.

## 3. 저장 방식 비교 및 선택 이유

| 방식 | 장점 | 비용 / 한계 | 이번 단계 판단 |
|---|---|---|---|
| **AsyncStorage + JSON** | Expo Go에서 사용 가능. 현재 `Todo[]` 상태를 JSON으로 직렬화해 저장하기 쉬움 | 목록 전체를 읽고 다시 저장. 복잡한 검색·대용량 데이터에는 불리. 암호화 저장소가 아님 | 단일 할 일 목록을 다루는 MP2에 선택 |
| SQLite (`expo-sqlite`) | 앱 재시작 후 유지되는 데이터베이스. SQL 검색과 구조화된 데이터 관리에 적합 | 테이블·쿼리·스키마 변경을 추가로 설계해야 함 | 항목·관계·검색 규모가 커질 때 검토 |

현재 앱은 한 기기의 비교적 작은 할 일 목록을 관리하며 이미 `useTodos`에 CRUD 로직이 모여 있습니다. AsyncStorage를 연결하면 기존 화면과 상태 관리 구조를 유지하면서 영구 저장 요구를 충족할 수 있습니다. 따라서 이번 단계에서는 구현 복잡도와 변경 범위를 줄일 수 있는 AsyncStorage를 선택했습니다. 실제 대용량 성능 벤치마크를 수행해 선택한 것은 아닙니다.

근거: [Expo SDK 54 AsyncStorage 문서](https://docs.expo.dev/versions/v54.0.0/sdk/async-storage/), [Expo SDK 54 SQLite 문서](https://docs.expo.dev/versions/v54.0.0/sdk/sqlite/).

### 저장 데이터와 처리 흐름

- 저장 키: `@dokdoki/todos:v1`
- 저장 형식: 전체 `Todo[]`의 JSON 문자열
- 항목: `id`, `text`, `done`, `dueDate`(선택), `category`, `createdAt`, `updatedAt`
- 카테고리 값: `none`, `school`, `personal`, `appointment`
- 현재 필터·검색어·정렬 선택은 화면 상태이며 재시작 후 초기값으로 돌아갑니다.

```text
앱 시작 → 저장 목록 읽기·형식 확인 → 화면에 복원
사용자 추가·수정·삭제·완료 변경 → 화면 갱신 → 변경 순서대로 저장
앱 재실행 → 같은 저장 키로 목록 복원
```

초기 읽기 전에 빈 목록을 저장하지 않습니다. 불러오는 중 발생한 변경은 대기했다가 복원된 목록 위에 적용합니다. 빠른 연속 조작은 저장 요청을 순서대로 처리해 오래된 목록이 최신 목록을 덮어쓰지 않도록 합니다.

읽기 실패나 손상된 JSON·잘못된 데이터 구조·중복 ID는 안내 후 재시도하며 기존 저장값을 자동 삭제하지 않습니다. 저장 실패 시 안내하고, 재시도하면 실패 당시 내용이 아닌 현재 최신 목록을 저장합니다. 삭제 후 목록이 비어도 `[]`를 저장합니다. 카테고리가 없는 이전 데이터나 알려지지 않은 카테고리는 내용과 완료 상태를 보존하고 ‘미분류’로 표시합니다.

## 4. 코드 구조

```text
App.js                         앱 시작 화면
hooks/useTodos.ts               CRUD·필터·검색·정렬·영구 저장
types/index.ts                 Todo·카테고리·필터 타입
constants/categories.ts        카테고리 이름·색상·기본값
components/CategoryPicker.tsx   추가·수정 화면 공용 선택기
components/AddTodoModal.tsx     할 일 추가
components/EditTodoModal.tsx    할 일 수정
components/TodoItem.tsx         항목·카테고리 표시
screens/TodoListScreen.tsx      목록 구성·전체 완료율
tests/mp2.test.cjs              MP2 자동 테스트
tests/helpers/runtime.cjs       테스트용 React/기기 기능 대체 환경
```

## 5. 검증

프로젝트 루트에서 다음을 실행합니다.

```bash
npm run typecheck
npm test
```

PowerShell에서는 `npm.cmd run typecheck`, `npm.cmd test`를 사용합니다.

2026-09-20 확인 결과:

- TypeScript 검사: 오류 0개.
- 자동 테스트: 15개 통과. 저장·복원, CRUD, 필터·검색, 공백 입력, 동일 제목의 별도 항목, 읽기·쓰기 오류, 손상 데이터 보존, 연속 저장 순서, 카테고리 선택·수정, 이전 데이터 호환, 전체 완료율을 확인했습니다.
- 자동 테스트는 실제 소스를 불러오고 휴대폰 저장소와 React Native 화면 기능을 테스트용 객체로 대체합니다. 실제 휴대폰의 화면 배치·키보드·네이티브 저장소까지 자동 검증한 것은 아닙니다.
- 갤럭시 Expo Go에서 개발자가 기존 CRUD·검색·필터, 재시작 후 데이터 유지, 전체 삭제 후 빈 목록 유지, 카테고리·완료율 추가 후 동작을 수동 확인했습니다.
- iOS 실기기 / 에뮬레이터에서 최종 MP2를 별도로 검증하지는 않았습니다.

실기기 재현 절차:

1. 서로 다른 카테고리로 테스트 항목을 추가합니다.
2. 한 항목을 완료하고 다른 항목의 내용·카테고리를 수정합니다.
3. 항목을 삭제하고 전체·완료·미완료 필터 및 검색을 확인합니다.
4. 완료율이 전체 목록 기준으로 변하는지 확인합니다.
5. 저장 시간을 두고 Expo Go를 완전히 종료했다가 다시 열어 내용·날짜·완료 상태·카테고리·삭제 결과를 확인합니다.
6. 전부 삭제한 뒤 재시작해도 빈 목록이 유지되는지 확인합니다.

## 6. 제출 산출물

| 산출물 | 내용 |
|---|---|
| `app/` | 앱 소스·설정·잠금 파일·테스트. 생성 폴더는 제외 |
| `README.md` | 기능, 실행 절차, 저장 방식과 대안 비교·선택 이유 |
| `prompts_log.md` | AI에 요청한 주요 내용과 채택·수정 내역 |
| `screenshots/` | 수정·삭제·필터·재시작 전후 및 팀 명세 추가 기능의 실제 캡처 |

캡처 파일은 개발자가 휴대폰에 보관한 실제 화면을 `screenshots/`에 넣어 첨부합니다. 파일명과 대응 기능은 `screenshots/README.md`를 참고합니다. **안내 파일만 있는 폴더는 화면 증빙을 대체하지 않습니다.**

## 7. 범위와 제한

- MP3의 BaaS 동기화·오프라인 서버 반영, MP4의 카메라·OCR는 이번 제출 범위 밖입니다. 기존 ‘메모 스캔’ 버튼과 설정 화면 일부 항목(로그아웃·데이터 초기화 등)은 아직 실제 기능과 연결되지 않은 후속 단계용 UI입니다.
- AsyncStorage는 암호화 저장소가 아닙니다. 비밀번호·토큰 등 민감정보 저장 용도로 사용하지 않습니다. [React Native 저장 보안 안내](https://reactnative.dev/docs/security)
- 데이터는 해당 앱 저장공간에 보관됩니다. 일반적인 앱 종료·재실행 후에는 유지되지만 앱 삭제·저장공간 초기화까지 보존하는 백업 기능은 없습니다.
- 저장은 비동기입니다. 저장 완료 전에 앱을 강제 종료한 변경까지 보존한다고 보장하지 않습니다.
- `.env`, 자격증명 파일, `node_modules`, `.expo`, `.git`은 제출 ZIP에 포함하지 않습니다.
- 서버 동기화 충돌 처리 방침은 MP3에서 서버 설계를 확정하면서 작성합니다. MP2는 단일 기기의 저장 순서만 관리합니다.
