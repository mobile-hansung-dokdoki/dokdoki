# 코드 리뷰 가이드 (code_review.md)

똑똑이 할 일 앱 (주제 #2109) — 팀 코드 리뷰 기준 문서
대상: React Native + Expo (TypeScript) / 팀 4인

이 문서는 두 가지 용도로 씁니다.
1. **남의 PR을 리뷰할 때** — 무엇을 확인할지 체크리스트로 사용
2. **내 코드를 올리기 전** — 셀프 리뷰로 미리 점검

---

## 리뷰 기본 원칙

- **작게, 자주**: PR은 이슈(티켓) 하나 단위로. 수백 줄짜리 PR은 리뷰가 안 됨.
- **티켓에 연결**: 모든 PR은 이슈 번호와 완료 기준(AC)에 연결. "이 PR이 AC를 만족하는가"가 첫 질문.
- **사람이 아니라 코드**: "너 왜 이렇게 했어"가 아니라 "이 부분은 이렇게 하면 어떨까". 근거를 같이.
- **막히면 대면/통화**: 코멘트가 3번 왕복하면 말로 푸는 게 빠름.

## 코멘트 레벨 표기 (팀 약속)

리뷰 코멘트 앞에 레벨을 붙여, 무엇이 필수이고 무엇이 취향인지 구분합니다.

- **[must]**  — 고쳐야 병합 가능 (버그, 기준 위반, 민감정보)
- **[should]** — 고치는 게 좋음 (설계·가독성 문제)
- **[nit]**   — 사소한 취향 (반영은 선택)
- **[q]**     — 질문 (이유가 궁금)
- **[praise]** — 잘한 부분 (칭찬도 리뷰다)

예) `[must] API 키가 코드에 그대로 있어요. .env로 빼야 합니다.`

---

## 공통 체크리스트 (모든 PR)

- [ ] 이슈 번호와 연결되어 있고, 완료 기준(AC)을 만족하는가
- [ ] 브랜치명이 규칙(`타입/이슈번호-설명`)을 따르는가
- [ ] 커밋이 의미 단위로 나뉘어 있는가
- [ ] 에러/경고 없이 실행되는가 (`npx expo start`)
- [ ] 한글 주석이 핵심 로직에 붙어 있는가 (팀 규칙)
- [ ] 안 쓰는 코드·`console.log`·주석 처리된 코드가 남아있지 않은가
- [ ] 이름이 역할을 드러내는가 (변수·함수·컴포넌트)

## TypeScript 체크리스트

- [ ] `any` 타입을 남용하지 않았는가 (불가피하면 이유 주석)
- [ ] 공용 타입(`Todo`)을 재정의하지 않고 `types/`에서 가져다 쓰는가
- [ ] 함수 인자·반환·props에 타입이 명시됐는가
- [ ] null/undefined 가능성이 처리됐는가 (옵셔널 체이닝 등)
- [ ] 타입 검사 통과 (`npx tsc --noEmit` 에러 0)

## React Native / 컴포넌트 체크리스트

- [ ] 상태를 직접 수정하지 않고 새 배열/객체로 교체했는가 (불변성)
      - 예: `setTodos([...todos, x])` / `todos.map(...)`  — `todos.push(x)` ❌
- [ ] 리스트에 안정적인 `key`(고유 id)를 쓰는가
- [ ] props로 필요한 것만 내려주는가 (과도한 전달 X)
- [ ] 화면-전용 값(입력 중 텍스트)과 공용 상태(todos)를 적절히 분리했는가
- [ ] 인라인 스타일 남발 대신 `StyleSheet`로 분리했는가
- [ ] 컴포넌트가 한 가지 일만 하는가 (너무 커지면 분리)

## 보안 / 민감정보 체크리스트 (⚠️ 제출 자동 검사 항목)

> 제출물 자동 검사에 "민감정보 스캔"이 있습니다. 여기서 걸리면 통과 실패.

- [ ] API 키·비밀번호가 코드에 하드코딩되어 있지 않은가
- [ ] 설정 키는 `.env`로 분리하고 `.env.example`만 커밋했는가 (MP3~)
- [ ] `README`·`prompts_log`에 개인정보(전화번호 등)가 없는가
- [ ] `.gitignore`에 `.env`, `node_modules`가 포함돼 있는가

## 단계별(MP) 특화 체크리스트

### MP1 — 화면 + 상태 관리
- [ ] 항목 추가 / 완료 토글이 실제로 동작
- [ ] 빈 상태 안내 화면이 표시됨
- [ ] 저장 로직이 들어가 있지 않은가 (MP1 범위 밖 — 넣지 말 것)

### MP2 — 저장 + CRUD + 필터
- [ ] 앱 재시작 후 데이터 유지
- [ ] 수정·삭제·필터 동작
- [ ] 저장 방식 선택 이유가 README에 대안 비교와 함께 있는가

### MP3 — 백엔드 동기화
- [ ] 기기 A 추가 → 기기 B 반영
- [ ] 오프라인 작성 → 온라인 복귀 시 서버 반영
- [ ] 동기화 충돌 처리 방침이 README에 있는가
- [ ] 비밀키 하드코딩 없이 `.env.example`만 제출

### MP4 — 온디바이스 OCR
- [ ] 사진 → 항목 후보 → 선택 저장 흐름 동작
- [ ] 비행기 모드에서도 인식 동작 (온디바이스 증명)
- [ ] 원본 사진을 앱에 저장하지 않음 / 권한 거부 안내 처리
- [ ] README에 처리 시간 측정값이 있는가

## 테스트 체크리스트 (구현 단계~)

- [ ] 핵심 로직(추가/토글/필터)에 단위 테스트가 있는가
- [ ] 테스트가 통과하는가 (`npm test`)
- [ ] 엣지 케이스(빈 입력, 공백, 중복)를 고려했는가

---

## 승인 기준

- **Approve (승인)**: [must] 없음 + AC 만족 + 실행·타입검사·테스트 통과
- **Request changes (변경 요청)**: [must]가 하나라도 있음
- **Comment (의견)**: [should]/[nit]만 있고 병합은 작성자 판단에 맡길 때

## 리뷰 기록 템플릿 (복사해서 사용)

```
### 리뷰: PR #[번호] — [제목]
- 리뷰어: [이름]
- 날짜: [YYYY-MM-DD]
- 연결 이슈: #[번호]
- 결과: [Approve / Request changes / Comment]

발견 사항
- [must]  ...
- [should] ...
- [nit]   ...
- [praise] ...

확인
- [ ] AC 만족   [ ] 실행 OK   [ ] 타입검사 OK   [ ] 민감정보 없음
```

---

## 부록 — 좋은 리뷰 코멘트 vs 나쁜 리뷰 코멘트

| 나쁜 예 | 좋은 예 |
|--------|--------|
| "이거 이상함" | "[should] 여기서 todos를 직접 push하면 화면이 안 바뀝니다. `setTodos([...todos, x])`로 바꿔주세요." |
| "왜 이렇게 함?" | "[q] 이 부분을 useState 대신 useRef로 둔 이유가 있을까요?" |
| (칭찬 없음) | "[praise] 빈 상태 컴포넌트 분리 깔끔하네요 👍" |
